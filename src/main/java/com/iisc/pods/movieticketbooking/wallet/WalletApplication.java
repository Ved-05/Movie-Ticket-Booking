package com.iisc.pods.movieticketbooking.wallet;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class WalletApplication {

    public static void main(String[] args) {
        SpringApplication.run(WalletApplication.class, args);
    }
}

// The WalletApplication class serves as the entry point for your Wallet
// service.
// It is annotated with @SpringBootApplication to enable Spring Boot features.
// The main method starts the Spring Boot application.